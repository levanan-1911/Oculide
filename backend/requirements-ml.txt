# ML / AI requirements for Celery proctoring workers
# Installed in Dockerfile.celery — NOT in the main backend image

# Pin torch BEFORE 2.6 to avoid weights_only UnpicklingError with ultralytics
torch==2.5.1
torchvision==0.20.1

# Computer Vision
# Use headless variant — no GUI deps needed inside Docker
opencv-python-headless==4.10.0.84

# Face / pose / gaze detection
mediapipe==0.10.18

# Object detection (phone, book, person count …)
ultralytics==8.3.50

# Numerical / image
numpy==1.26.4
Pillow==10.4.0
